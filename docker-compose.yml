services:
  game-portal:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: frybahn
    ports:
      - "90:90"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:90/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
    labels:
      - "com.frybahn.description=Frybahn Browser Gaming Portal"
      - "com.frybahn.version=1.0.0"
